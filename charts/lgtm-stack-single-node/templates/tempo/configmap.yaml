apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lgtm-stack.componentConfig" . }}
  labels:
    app: tempo
data:
  tempo.yaml: |
{{ include "lgtm-stack.TempoDefaultConfig" . | indent 4 }}
