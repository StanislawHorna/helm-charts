apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lgtm-stack.componentConfig" . }}
  labels:
    app: mimir
data:
  config.yaml: |
{{ include "lgtm-stack.MimirDefaultConfig" . | indent 4 }}
