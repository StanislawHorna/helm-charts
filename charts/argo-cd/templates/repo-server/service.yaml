apiVersion: v1
kind: Service
metadata:
  name: argocd-repo-server
  labels:
    {{- include "argocd.labels" . | nindent 4 }}
    app.kubernetes.io/name: argocd-repo-server
spec:
  ports:
    - port: 8081
      targetPort: 8081
      name: http
  selector:
    app.kubernetes.io/name: argocd-repo-server
