apiVersion: v2
name: vault-integration
description: HashiCorp Vault integration with k8s using cert manager and external secrets.
type: application
version: 0.0.1
appVersion: "1.0.0"

dependencies:
  - name: external-secrets
    version: 2.1.0
    repository: "https://charts.external-secrets.io"