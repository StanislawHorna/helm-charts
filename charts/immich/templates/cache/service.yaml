apiVersion: v1
kind: Service
metadata:
  name: {{ include "immich.cache.serviceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "immich.cache.appName" . }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app: {{ include "immich.cache.appName" . }}